# Rate-limit zones for the HIPAA app (http context).
# Install to /etc/nginx/conf.d/hipaa-ratelimit.conf
limit_req_zone $binary_remote_addr zone=hipaa_login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=hipaa_api:10m rate=20r/s;

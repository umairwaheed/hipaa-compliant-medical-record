# Python
backend/.venv/
__pycache__/
*.pyc

# Databases (may contain PHI — never commit)
*.db
*.sqlite
*.sqlite3

# Secrets — NEVER commit. Local recovery material (Vault unseal keys, tokens,
# credentials) lives here and must stay untracked.
.env
backend/.env
/secrets/
*.local.md
*.secret

# Node
frontend/node_modules/
frontend/dist/

# OS / editor
.DS_Store
.vscode/
.idea/
